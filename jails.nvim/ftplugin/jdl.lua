-- Editing a `.jails/model.jdl` buffer. The settings themselves live in
-- `jails.configure_jdl_buffer()` beside the Java ones, so the plugin keeps one
-- place where buffer configuration is decided.
require('jails').configure_jdl_buffer()
