/target

# Local reference checkouts used for research only -- each is its own
# separate git repo, never part of this one. deps/deps.tsv and
# deps/update.sh ARE tracked; the checkouts they manage are not.
/deps/*/
