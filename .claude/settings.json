{
  "hooks": {
    "Stop": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "cd \"${CLAUDE_PROJECT_DIR:-.}\" && mise run lint >/tmp/jails-stop-hook.log 2>&1 || echo '{\"systemMessage\": \"jails: `mise run lint` failed after this turn -- see /tmp/jails-stop-hook.log; run `mise run verify-rewrite` before pushing\"}'",
            "timeout": 300,
            "statusMessage": "mise run lint (jails)"
          }
        ]
      }
    ],
    "SessionStart": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "$CLAUDE_PROJECT_DIR/.claude/hooks/session-start.sh",
            "timeout": 900,
            "statusMessage": "installing the pinned JDK, Maven, Gradle and container engine (jails)"
          }
        ]
      }
    ]
  }
}
