apiVersion: v1
kind: Service
metadata:
  name: {{NAME}}
spec:
  selector:
    app.kubernetes.io/name: {{NAME}}
  ports:
    - name: http
      port: 80
      targetPort: http
    - name: o11y
      port: 8081
      targetPort: o11y
