[package]
name = "jails-codec-derive"
version.workspace = true
edition.workspace = true

[lib]
proc-macro = true

[dependencies]
# The three standard proc-macro crates and nothing else. One canonical framing
# shared by every closed format, derived from the declaration.
proc-macro2 = "1"
quote = "1"
syn = { version = "2", features = ["full"] }

[lints]
workspace = true
