# `jails-protocol`

The previous plan/transition/effect vocabulary. The parts the surviving crates
still use -- the `database` vocabulary, `MavenCoordinate`, the rename and
type-change policies, the field-syntax parser -- move to `jails-spec` under
`docs/51-kernel.md` S51.2; the rest is deleted with the kernel.
