[package]
name = "jails-protocol"
version.workspace = true
edition.workspace = true

[dependencies]
jails-codec-derive.workspace = true
jails-codemod.workspace = true
# Recipe and Capability are `ValueEnum`s: their CLI spelling is their recorded
# spelling, through clap's own table rather than a second match.
clap.workspace = true
# `SqlName`'s two conventional spellings go through the one `snake_case`, at
# the layer that owns identifier spelling.
jails-java.workspace = true
jails-spec.workspace = true
jails-support.workspace = true

[lints]
workspace = true
