# `jails-commit`

The previous transaction executor: object store, journal, receipts, recovery
and garbage collection under `.jails/`. Nothing the binary can create reaches
it. `docs/51-kernel.md` deletes it.
