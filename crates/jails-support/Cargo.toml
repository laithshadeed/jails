[package]
name = "jails-support"
version.workspace = true
edition.workspace = true

[dependencies]
fs2.workspace = true
# The `#[derive(Codec)]` proc macro, a leaf crate with only the three standard
# proc-macro dependencies. `identity` lives here and every one of its values is
# a closed wire format, so the derive has to sit below it.
jails-codec-derive.workspace = true
nix.workspace = true
# plan.md §3.2: atomic, random, cross-platform exclusive directory creation and
# owned cleanup. A normal dependency rather than a dev one, because
# reconciliation and R3 preparation need the guard in production -- and
# reimplementing OS randomness and Windows deletion semantics is not this
# project's work.
tempfile.workspace = true

[lints]
workspace = true
