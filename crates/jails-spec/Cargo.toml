[package]
name = "jails-spec"
version.workspace = true
edition.workspace = true

[dependencies]
# The closed vocabularies live in `jails-model`; `suffix` matches on the
# generator kind, which is one of them.
jails-model.workspace = true
jails-support.workspace = true

[lints]
workspace = true
