[package]
name = "jails-spec"
version.workspace = true
edition.workspace = true

[dependencies]
# `Capability` is a `clap::ValueEnum` so `clap_complete` can emit a static
# completion list for it -- see CLAUDE.md. The derive has to live with the enum.
clap = { version = "4.6.4", features = ["derive"] }
jails-support = { path = "../jails-support" }
jails-java = { path = "../jails-java" }

[lints]
workspace = true
