# `jails-generate`

What remains of the previous generator: the byte-keyed write path
(`generate::write_new_file`) and the field spec's SQL projection (`sql`). The
compiler emits every kind and capability; `docs/53-tool-crates.md` S53.4
deletes this crate once its two callers use the compiler's answers.
