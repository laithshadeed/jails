// Generated by jails from art_cmp_http_workflow_site_walk_controller. Clean hand edits survive regeneration.
package com.example.demo.web;

import com.example.demo.jobs.SiteWalkWorkflow;
import java.util.List;
import java.util.UUID;
import org.springframework.http.HttpStatus;
import org.springframework.web.bind.annotation.DeleteMapping;
import org.springframework.web.bind.annotation.GetMapping;
import org.springframework.web.bind.annotation.PathVariable;
import org.springframework.web.bind.annotation.PostMapping;
import org.springframework.web.bind.annotation.RequestBody;
import org.springframework.web.bind.annotation.RequestMapping;
import org.springframework.web.bind.annotation.ResponseStatus;
import org.springframework.web.bind.annotation.RestController;
import org.springframework.web.server.ResponseStatusException;

/** HTTP control plane for the durable SiteWalk workflow. */
@RestController
@RequestMapping(SiteWalkWorkflowController.PATH)
public final class SiteWalkWorkflowController {

    public static final String PATH = "/workflows/site-walk";
    private final SiteWalkWorkflow workflow;

    public SiteWalkWorkflowController(SiteWalkWorkflow workflow) {
        this.workflow = workflow;
    }

    @PostMapping
    @ResponseStatus(HttpStatus.ACCEPTED)
    public SiteWalkWorkflow.RunStatus start(@RequestBody SiteWalkWorkflow.StartRequest request) {
        try { return workflow.start(request); }
        catch (IllegalArgumentException invalid) {
            throw new ResponseStatusException(HttpStatus.BAD_REQUEST, invalid.getMessage(), invalid);
        } catch (SiteWalkWorkflow.IdempotencyConflictException conflict) {
            throw new ResponseStatusException(HttpStatus.CONFLICT, conflict.getMessage(), conflict);
        }
    }

    @GetMapping("/{id}")
    public SiteWalkWorkflow.RunStatus status(@PathVariable UUID id) {
        return workflow.status(id).orElseThrow(() ->
                new ResponseStatusException(HttpStatus.NOT_FOUND, "workflow run not found"));
    }

    @GetMapping("/{id}/pages")
    public List<SiteWalkWorkflow.Page> pages(@PathVariable UUID id) {
        if (workflow.status(id).isEmpty()) {
            throw new ResponseStatusException(HttpStatus.NOT_FOUND, "workflow run not found");
        }
        return workflow.pages(id);
    }

    @DeleteMapping("/{id}")
    public SiteWalkWorkflow.RunStatus cancel(@PathVariable UUID id) {
        try { return workflow.cancel(id); }
        catch (SiteWalkWorkflow.NotFoundException missing) {
            throw new ResponseStatusException(HttpStatus.NOT_FOUND, missing.getMessage(), missing);
        }
    }
}
