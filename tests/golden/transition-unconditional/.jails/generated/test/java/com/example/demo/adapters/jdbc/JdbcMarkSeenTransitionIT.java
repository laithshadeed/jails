// Generated by jails from art_op_mark_seen_write_it. Clean hand edits survive regeneration.
package com.example.demo.adapters.jdbc;

import static org.assertj.core.api.Assertions.assertThat;

import com.example.demo.TestcontainersConfig;
import com.example.demo.application.transitions.MarkSeenTransition;
import com.example.demo.domain.Note;
import com.example.demo.repository.NoteRepository;
import org.junit.jupiter.api.Test;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.boot.test.context.SpringBootTest;
import org.springframework.context.annotation.Import;
import org.springframework.transaction.annotation.Transactional;

@Import(TestcontainersConfig.class)
@SpringBootTest
@Transactional
class JdbcMarkSeenTransitionIT {

    @Autowired
    private NoteRepository repository;

    @Autowired
    private MarkSeenTransition operation;

    @Test
    void writesThroughTheRealDatabase() {
        Note stored = repository.save(new Note(1L, "sample", false, 1L));
        Note answered = operation.execute(stored.id(), new MarkSeenTransition.Input(), stored.version());

        // `returning` answers with the row the statement wrote, so an empty
        // answer here means it matched none -- which is the failure worth
        // catching.
        assertThat(answered).isNotNull();
    }

    @Test
    void aCallerThatSendsNoPreconditionAppliesUnconditionallyAndCanRepeat() {
        Note stored = repository.save(new Note(1L, "sample", false, 1L));
        Note first = operation.execute(stored.id(), new MarkSeenTransition.Input(), null);
        Note again = operation.execute(stored.id(), new MarkSeenTransition.Input(), null);

        assertThat(first).isNotNull();
        assertThat(again).isNotNull();
    }

    // Reader-owned cases belong below this stable boundary.
}
