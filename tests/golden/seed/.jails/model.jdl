jdl 1
app Demo @id(project_demo) {
  pkg com.example.demo
  java 26
  platform spring
  build maven
  storage postgres
}

cap json @id(cap_json)

entity Widget @id(ent_widget) {
  use scaffold
  use seed

  id: uuid @id(fld_widget_id) @pk
  name: string @id(fld_widget_name) @notBlank
}
