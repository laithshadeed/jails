// Generated by jails from art_ent_widget_http_controller_test. Clean hand edits survive regeneration.
package com.example.demo.web;

import static org.assertj.core.api.Assertions.assertThat;

import com.example.demo.domain.Widget;
import com.example.demo.repository.WidgetRepository;
import com.example.demo.service.WidgetService;
import java.util.List;
import java.util.Optional;
import java.util.UUID;
import org.junit.jupiter.api.Test;
import org.springframework.http.MediaType;
import org.springframework.test.web.servlet.assertj.MockMvcTester;

class WidgetControllerTest {

    private static final Widget ROW = new Widget(UUID.fromString("00000000-0000-0000-0000-000000000001"), "sample");

    private static final WidgetRepository REPOSITORY = new WidgetRepository() {
        @Override
        public Optional<Widget> findById(UUID id) {
            return Optional.of(ROW);
        }

        @Override
        public List<Widget> findAll() {
            return List.of(ROW);
        }

        @Override
        public Widget save(Widget value) {
            return value;
        }

        @Override
        public boolean deleteById(UUID id) {
            return true;
        }
    };

    private static final String CREATE_REQUEST =
            """
            {
              "name": "sample-name"
            }""";

    private final MockMvcTester mvc = MockMvcTester.of(new WidgetController(new WidgetService(REPOSITORY)));

    @Test
    void theDocumentedCreateRequestIsAccepted() {
        assertThat(mvc.post().uri("/widgets")
                .contentType(MediaType.APPLICATION_JSON)
                .content(CREATE_REQUEST)).hasStatus(201);
        assertThat(mvc.get().uri("/widgets")).hasStatusOk();
    }

    // Reader-owned tests belong below this stable boundary.
}
