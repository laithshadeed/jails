// Generated by jails from art_ent_widget_service. Clean hand edits survive regeneration.
package com.example.demo.service;

import com.example.demo.domain.Widget;
import com.example.demo.repository.WidgetRepository;
import java.util.List;
import java.util.Objects;
import java.util.Optional;
import java.util.UUID;
import org.springframework.stereotype.Component;

/**
 * What the application can do with {@link Widget}.
 *
 * <p>Depends on the port, not on an adapter, so a test can hand it an
 * in-memory implementation and never start a database.
 */
@Component
public class WidgetService {

    private final WidgetRepository repository;

    public WidgetService(WidgetRepository repository) {
        this.repository = Objects.requireNonNull(repository, "repository is required");
    }

    public Optional<Widget> byId(UUID id) {
        return repository.findById(id);
    }

    public List<Widget> all() {
        return repository.findAll();
    }

    public Widget save(Widget widget) {
        return repository.save(widget);
    }

    public boolean delete(UUID id) {
        return repository.deleteById(id);
    }

    // Reader-owned application methods belong below this stable boundary.
}
