// Generated by jails from art_ent_item_http_controller_test. Clean hand edits survive regeneration.
package com.example.demo.web;

import com.example.demo.domain.Item;
import com.example.demo.repository.ItemRepository;
import com.example.demo.service.ItemService;
import java.time.Instant;
import java.util.List;
import java.util.Optional;
import java.util.UUID;
import org.junit.jupiter.api.Test;
import org.springframework.http.MediaType;
import org.springframework.test.web.servlet.assertj.MockMvcTester;
import static org.assertj.core.api.Assertions.assertThat;

class ItemControllerTest {

    private static final Item ROW = new Item(UUID.fromString("00000000-0000-0000-0000-000000000001"), UUID.fromString("00000000-0000-0000-0000-000000000001"), "sample", Instant.parse("2026-01-01T00:00:00Z"));

    private static final ItemRepository REPOSITORY = new ItemRepository() {
        @Override
        public Optional<Item> findById(UUID id) {
            return Optional.of(ROW);
        }

        @Override
        public List<Item> findAll() {
            return List.of(ROW);
        }

        @Override
        public Item save(Item value) {
            return value;
        }

        @Override
        public boolean deleteById(UUID id) {
            return true;
        }
    };

    private static final String CREATE_REQUEST =
            """
            {
              "ownerId": "00000000-0000-0000-0000-000000000001",
              "name": "sample-name"
            }""";

    private final MockMvcTester mvc = MockMvcTester.of(new ItemController(new ItemService(REPOSITORY)));

    @Test
    void theDocumentedCreateRequestIsAccepted() {
        assertThat(mvc.post().uri("/items")
                .contentType(MediaType.APPLICATION_JSON)
                .content(CREATE_REQUEST)).hasStatus(201);
        assertThat(mvc.get().uri("/items")).hasStatusOk();
    }

    // Reader-owned tests belong below this stable boundary.
}
