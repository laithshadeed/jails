// Generated by jails from art_op_add_item_write_it. Clean hand edits survive regeneration.
package com.example.demo.adapters.jdbc;

import static org.assertj.core.api.Assertions.assertThat;

import com.example.demo.TestcontainersConfig;
import com.example.demo.application.commands.AddItemCommand;
import com.example.demo.domain.Item;
import com.example.demo.domain.Owner;
import com.example.demo.repository.OwnerRepository;
import java.time.Instant;
import java.util.UUID;
import org.junit.jupiter.api.Test;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.boot.test.context.SpringBootTest;
import org.springframework.context.annotation.Import;
import org.springframework.transaction.annotation.Transactional;

@Import(TestcontainersConfig.class)
@SpringBootTest
@Transactional
class JdbcAddItemCommandIT {

    @Autowired
    private OwnerRepository ownerRepository;

    @Autowired
    private AddItemCommand operation;

    @Test
    void writesThroughTheRealDatabase() {
        Owner ownerRow = ownerRepository.save(new Owner(UUID.fromString("00000000-0000-0000-0000-000000000001"), "sample", Instant.parse("2026-01-01T00:00:00Z")));
        Item answered = operation.execute(new AddItemCommand.Input(UUID.fromString("00000000-0000-0000-0000-000000000001"), ownerRow.id(), "sample"));

        // `returning` answers with the row the statement wrote, so an empty
        // answer here means it matched none -- which is the failure worth
        // catching.
        assertThat(answered).isNotNull();
    }

    // Reader-owned cases belong below this stable boundary.
}
