// Generated by jails from art_op_set_topic_subject_transition. Clean hand edits survive regeneration.
package com.example.demo.application.transitions;

import com.example.demo.domain.Topic;
import java.util.Objects;

public interface SetTopicSubjectTransition {
    String ROUTE = "PATCH /admin_api/topics/{userId}/subject";

    Topic execute(long userId, Input input);

    public record Input(
        String subject
    ) {

        public Input {
            Objects.requireNonNull(subject, "subject");
            subject = subject.trim();
            if (subject.isEmpty()) {
                throw new IllegalArgumentException("subject must not be blank");
            }
        }
    }
}
