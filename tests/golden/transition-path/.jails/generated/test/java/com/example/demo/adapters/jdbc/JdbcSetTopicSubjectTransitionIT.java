// Generated by jails from art_op_set_topic_subject_write_it. Clean hand edits survive regeneration.
package com.example.demo.adapters.jdbc;

import static org.assertj.core.api.Assertions.assertThat;

import com.example.demo.TestcontainersConfig;
import com.example.demo.application.transitions.SetTopicSubjectTransition;
import com.example.demo.domain.Topic;
import com.example.demo.repository.TopicRepository;
import org.junit.jupiter.api.Test;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.boot.test.context.SpringBootTest;
import org.springframework.context.annotation.Import;
import org.springframework.transaction.annotation.Transactional;

@Import(TestcontainersConfig.class)
@SpringBootTest
@Transactional
class JdbcSetTopicSubjectTransitionIT {

    @Autowired
    private TopicRepository repository;

    @Autowired
    private SetTopicSubjectTransition operation;

    @Test
    void writesThroughTheRealDatabase() {
        Topic stored = repository.save(new Topic(1L, 1L, "sample", 1L));
        Topic answered = operation.execute(stored.userId(), new SetTopicSubjectTransition.Input("sample"), stored.version());

        // `returning` answers with the row the statement wrote, so an empty
        // answer here means it matched none -- which is the failure worth
        // catching.
        assertThat(answered).isNotNull();
    }

    // Reader-owned cases belong below this stable boundary.
}
