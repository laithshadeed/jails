// Generated by jails from art_ent_topic_http_controller_test. Clean hand edits survive regeneration.
package com.example.demo.web;

import static org.assertj.core.api.Assertions.assertThat;

import com.example.demo.domain.Topic;
import com.example.demo.repository.TopicRepository;
import com.example.demo.service.TopicService;
import java.util.List;
import java.util.Optional;
import org.junit.jupiter.api.Test;
import org.springframework.http.MediaType;
import org.springframework.test.web.servlet.assertj.MockMvcTester;

class TopicControllerTest {

    private static final Topic ROW = new Topic(1L, 1L, "sample", 1L);

    private static final TopicRepository REPOSITORY = new TopicRepository() {
        @Override
        public Optional<Topic> findById(long id) {
            return Optional.of(ROW);
        }

        @Override
        public List<Topic> findAll() {
            return List.of(ROW);
        }

        @Override
        public Topic save(Topic value) {
            return value;
        }

        @Override
        public boolean deleteById(long id) {
            return true;
        }
    };

    private static final String CREATE_REQUEST =
            """
            {
              "userId": 1,
              "subject": "sample-subject"
            }""";

    private final MockMvcTester mvc = MockMvcTester.of(new TopicController(new TopicService(REPOSITORY)));

    @Test
    void theDocumentedCreateRequestIsAccepted() {
        assertThat(mvc.post().uri("/topics")
                .contentType(MediaType.APPLICATION_JSON)
                .content(CREATE_REQUEST))
                .hasStatus(201);
        assertThat(mvc.get().uri("/topics"))
                .hasStatusOk();
    }

    // Reader-owned tests belong below this stable boundary.
}
