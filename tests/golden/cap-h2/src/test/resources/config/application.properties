# Tests get their own in-memory database. Inheriting the file URL above
# would write into the working tree, and would fail on H2's file lock the
# moment the suite ran while the application was up.
spring.datasource.url=jdbc:h2:mem:test;DB_CLOSE_DELAY=-1
