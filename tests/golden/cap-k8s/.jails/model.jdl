jdl 1
app Demo @id(project_demo) {
  pkg com.example.demo
  java 26
  platform spring
  build maven
  storage none
}

cap actuator @id(cap_actuator)

cap observability @id(cap_observability)

cap docker @id(cap_docker)

cap k8s @id(cap_k8s)
