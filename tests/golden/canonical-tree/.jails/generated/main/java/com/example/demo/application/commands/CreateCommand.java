// Generated by jails from art_op_note_create_command. Clean hand edits survive regeneration.
package com.example.demo.application.commands;

import com.example.demo.domain.Note;
import java.util.Objects;

public interface CreateCommand {
    String ROUTE = "POST /actions/create";

    Note execute(Input input);

    public record Input(
        String title,
        String status
    ) {
    
        public Input {
            Objects.requireNonNull(title, "title");
            title = title.trim();
            if (title.isEmpty()) {
                throw new IllegalArgumentException("title must not be blank");
            }
            Objects.requireNonNull(status, "status");
        }
    }
}
