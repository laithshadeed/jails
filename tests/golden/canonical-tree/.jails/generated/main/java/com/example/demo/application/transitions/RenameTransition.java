// Generated by jails from art_op_note_rename_transition. Clean hand edits survive regeneration.
package com.example.demo.application.transitions;

import com.example.demo.domain.Note;
import java.util.Objects;
import java.util.UUID;

public interface RenameTransition {
    String ROUTE = "PATCH /actions/rename/{id}";

    Note execute(UUID id, Input input);

    public record Input(
        String title
    ) {
    
        public Input {
            Objects.requireNonNull(title, "title");
            title = title.trim();
            if (title.isEmpty()) {
                throw new IllegalArgumentException("title must not be blank");
            }
        }
    }
}
