// Generated by jails from art_op_note_create_adapter_test. Clean hand edits survive regeneration.
package com.example.demo.adapters.jdbc;

import com.example.demo.TestcontainersConfig;
import com.example.demo.application.commands.CreateCommand;
import com.example.demo.domain.Note;
import org.junit.jupiter.api.Test;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.boot.test.context.SpringBootTest;
import org.springframework.context.annotation.Import;
import org.springframework.transaction.annotation.Transactional;
import static org.assertj.core.api.Assertions.assertThat;

@Import(TestcontainersConfig.class)
@SpringBootTest
@Transactional
class JdbcCreateCommandIT {

    @Autowired
    private CreateCommand operation;

    @Test
    void runsAgainstTheRealDatabase() {
        Note answered = operation.execute(new CreateCommand.Input("sample", "sample"));

        assertThat(answered).isNotNull();
    }

    // Reader-owned tests belong below this stable boundary.
}
