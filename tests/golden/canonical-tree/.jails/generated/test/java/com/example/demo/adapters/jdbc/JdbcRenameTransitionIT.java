// Generated by jails from art_op_note_rename_adapter_test. Clean hand edits survive regeneration.
package com.example.demo.adapters.jdbc;

import com.example.demo.TestcontainersConfig;
import com.example.demo.application.transitions.RenameTransition;
import com.example.demo.domain.Note;
import com.example.demo.repository.NoteRepository;
import java.util.UUID;
import org.junit.jupiter.api.Test;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.boot.test.context.SpringBootTest;
import org.springframework.context.annotation.Import;
import org.springframework.transaction.annotation.Transactional;
import static org.assertj.core.api.Assertions.assertThat;

@Import(TestcontainersConfig.class)
@SpringBootTest
@Transactional
class JdbcRenameTransitionIT {

    @Autowired
    private RenameTransition operation;

    @Autowired
    private NoteRepository noteRepository;

    @Test
    void runsAgainstTheRealDatabase() {
        Note stored = noteRepository.save(new Note(UUID.fromString("00000000-0000-0000-0000-000000000001"), "sample", "sample"));
        Note answered = operation.execute(stored.id(), new RenameTransition.Input("sample"));

        assertThat(answered).isNotNull();
    }

    // Reader-owned tests belong below this stable boundary.
}
