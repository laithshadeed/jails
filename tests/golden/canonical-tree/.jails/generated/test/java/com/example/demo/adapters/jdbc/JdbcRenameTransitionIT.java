// Generated by jails from art_op_note_rename_write_it. Clean hand edits survive regeneration.
package com.example.demo.adapters.jdbc;

import static org.assertj.core.api.Assertions.assertThat;

import com.example.demo.TestcontainersConfig;
import com.example.demo.application.transitions.RenameTransition;
import com.example.demo.domain.Note;
import com.example.demo.repository.NoteRepository;
import java.util.UUID;
import org.junit.jupiter.api.Test;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.boot.test.context.SpringBootTest;
import org.springframework.context.annotation.Import;
import org.springframework.transaction.annotation.Transactional;

@Import(TestcontainersConfig.class)
@SpringBootTest
@Transactional
class JdbcRenameTransitionIT {

    @Autowired
    private NoteRepository repository;

    @Autowired
    private RenameTransition operation;

    @Test
    void writesThroughTheRealDatabase() {
        Note stored = repository.save(new Note(UUID.fromString("00000000-0000-0000-0000-000000000001"), "sample", "sample"));
        Note answered = operation.execute(stored.id(), new RenameTransition.Input("sample"));

        // `returning` answers with the row the statement wrote, so a null
        // here means it matched none -- which is the failure worth catching.
        assertThat(answered).isNotNull();
    }

    // Reader-owned cases belong below this stable boundary.
}
