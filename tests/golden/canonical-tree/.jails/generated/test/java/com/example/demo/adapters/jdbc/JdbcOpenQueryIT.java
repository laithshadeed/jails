// Generated by jails from art_op_note_open_query_it. Clean hand edits survive regeneration.
package com.example.demo.adapters.jdbc;

import static org.assertj.core.api.Assertions.assertThat;

import com.example.demo.TestcontainersConfig;
import com.example.demo.application.queries.OpenQuery;
import com.example.demo.domain.Note;
import com.example.demo.repository.NoteRepository;
import java.util.UUID;
import org.junit.jupiter.api.Test;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.boot.test.context.SpringBootTest;
import org.springframework.context.annotation.Import;
import org.springframework.transaction.annotation.Transactional;

@Import(TestcontainersConfig.class)
@SpringBootTest
@Transactional
class JdbcOpenQueryIT {

    @Autowired
    private NoteRepository repository;

    @Autowired
    private OpenQuery query;

    @Test
    void answersFromTheRealDatabase() {
        // The *stored* row, not the argument: with a database-assigned key
        // the two differ by exactly the column the query reads back.
        Note stored = repository.save(new Note(UUID.fromString("00000000-0000-0000-0000-000000000001"), "sample", "sample"));

        var found = query.execute(new OpenQuery.Input("sample"));

        assertThat(found).contains(stored);
    }

    // Reader-owned cases belong below this stable boundary.
}
