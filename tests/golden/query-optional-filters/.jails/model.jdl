jdl 1
app Demo @id(project_demo) {
  pkg com.example.demo
  java 26
  platform spring
  build maven
  storage postgres
}

entity Ticket @id(ent_ticket) {
  use scaffold

  id: long @id(fld_ticket_id) @pk
  status: string @id(fld_ticket_status) @notBlank
  category: string? @id(fld_ticket_category)
  query TicketsByStatus(status, category?) @id(op_tickets_by_status) {
  }
}
