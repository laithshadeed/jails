// Generated by jails from art_ent_ticket_dto_request. Clean hand edits survive regeneration.
package com.example.demo.web;

import com.example.demo.domain.Ticket;
import jakarta.validation.constraints.NotBlank;
import java.util.Optional;

public record TicketRequest(
    @NotBlank String status,
    String category
) {

    /**
     * The domain row this request describes, with every server-assigned
     * value supplied here rather than taken from the caller.
     */
    public Ticket toDomain() {
        return new Ticket(
                0L,
                status,
                Optional.ofNullable(category));
    }
}
