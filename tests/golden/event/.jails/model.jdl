jdl 1
app Demo @id(project_demo) {
  pkg com.example.demo
  java 26
  platform spring
  build maven
  storage none
}

cap kafka @id(cap_kafka)

entity Transaction @id(ent_transaction) {
  id: uuid @id(fld_transaction_id)
  amount: long @id(fld_transaction_amount)
  event TransactionSettled(id: uuid) @id(op_transaction_settled) {
  }
}
