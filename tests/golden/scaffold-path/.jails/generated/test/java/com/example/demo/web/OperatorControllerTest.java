// Generated by jails from art_ent_operator_http_controller_test. Clean hand edits survive regeneration.
package com.example.demo.web;

import static org.assertj.core.api.Assertions.assertThat;

import com.example.demo.domain.Operator;
import com.example.demo.repository.OperatorRepository;
import com.example.demo.service.OperatorService;
import java.util.List;
import java.util.Optional;
import org.junit.jupiter.api.Test;
import org.springframework.http.MediaType;
import org.springframework.test.web.servlet.assertj.MockMvcTester;

class OperatorControllerTest {

    private static final Operator ROW = new Operator(1L, "sample");

    private static final OperatorRepository REPOSITORY = new OperatorRepository() {
        @Override
        public Optional<Operator> findById(long id) {
            return Optional.of(ROW);
        }

        @Override
        public List<Operator> findAll() {
            return List.of(ROW);
        }

        @Override
        public Operator save(Operator value) {
            return value;
        }

        @Override
        public boolean deleteById(long id) {
            return true;
        }
    };

    private static final String CREATE_REQUEST =
            """
            {
              "email": "sample-email"
            }""";

    private final MockMvcTester mvc = MockMvcTester.of(new OperatorController(new OperatorService(REPOSITORY)));

    @Test
    void theDocumentedCreateRequestIsAccepted() {
        assertThat(mvc.post().uri("/admin_api/operators")
                .contentType(MediaType.APPLICATION_JSON)
                .content(CREATE_REQUEST))
                .hasStatus(201);
        assertThat(mvc.get().uri("/admin_api/operators"))
                .hasStatusOk();
    }

    // Reader-owned tests belong below this stable boundary.
}
