// Generated by jails from art_cap_db_op_mark_note_seen_transition. Clean hand edits survive regeneration.
package com.example.demo.adapters.jdbc;

import com.example.demo.application.transitions.MarkNoteSeenTransition;
import com.example.demo.domain.Note;
import java.util.ArrayList;
import java.util.List;
import org.springframework.jdbc.core.simple.JdbcClient;
import org.springframework.stereotype.Repository;
import org.springframework.transaction.annotation.Transactional;

@Repository
public class JdbcMarkNoteSeenTransition implements MarkNoteSeenTransition {

    private final JdbcClient jdbc;

    public JdbcMarkNoteSeenTransition(JdbcClient jdbc) {
        this.jdbc = jdbc;
    }

    @Override
    @Transactional
    public Note execute(long id, MarkNoteSeenTransition.Input input) {
        var predicates = new ArrayList<>(List.of("id = :id", "version = :guard_version"));
        var sql = "update notes set seen = true, version = version + 1 where " + String.join(" and ", predicates) + " returning id, body, seen, version";
        JdbcClient.StatementSpec statement = jdbc.sql(sql);
        statement = statement.param("id", id);
        statement = statement.param("guard_version", input.version());
        return statement.query(Note.class).single();
    }
}
