// Generated by jails from art_ent_note_http_controller_test. Clean hand edits survive regeneration.
package com.example.demo.web;

import com.example.demo.domain.Note;
import com.example.demo.repository.NoteRepository;
import com.example.demo.service.NoteService;
import java.time.Instant;
import java.util.List;
import java.util.Optional;
import java.util.UUID;
import org.junit.jupiter.api.Test;
import org.springframework.http.MediaType;
import org.springframework.test.web.servlet.assertj.MockMvcTester;
import static org.assertj.core.api.Assertions.assertThat;

class NoteControllerTest {

    private static final Note ROW = new Note(UUID.fromString("00000000-0000-0000-0000-000000000001"), "sample", Instant.parse("2026-01-01T00:00:00Z"));

    private static final NoteRepository REPOSITORY = new NoteRepository() {
        @Override
        public Optional<Note> findById(UUID id) {
            return Optional.of(ROW);
        }

        @Override
        public List<Note> findAll() {
            return List.of(ROW);
        }

        @Override
        public Note save(Note value) {
            return value;
        }

        @Override
        public boolean deleteById(UUID id) {
            return true;
        }
    };

    private static final String CREATE_REQUEST =
            """
            {
              "title": "sample-title",
              "createdAt": "2026-01-01T00:00:00Z"
            }""";

    private final MockMvcTester mvc = MockMvcTester.of(new NoteController(new NoteService(REPOSITORY)));

    @Test
    void theDocumentedCreateRequestIsAccepted() {
        assertThat(mvc.post().uri("/notes")
                .contentType(MediaType.APPLICATION_JSON)
                .content(CREATE_REQUEST)).hasStatus(201);
        assertThat(mvc.get().uri("/notes")).hasStatusOk();
    }

    // Reader-owned tests belong below this stable boundary.
}
