# jails' own bookkeeping. Written by jails, never hand-edited -- `jails.toml`
# and `.jails/app.toml` are the files that are yours.
#
# Identity is (recipe, name, package). Everything else is content, which is
# what makes an edited intent an update to a known entity rather than a new
# one arriving against files that already exist.
version = "0.1.0"

[[applied]]
recipe = "class"
name = "RingBuffer"
package = ""
has_spec = false
files = [
  "src/main/java/com/example/demo/RingBuffer.java",
  "src/test/java/com/example/demo/RingBufferTest.java",
]

[[applied]]
recipe = "integration-test"
name = "Checkout"
package = ""
has_spec = false
files = [
  "src/test/java/com/example/demo/CheckoutIT.java",
]

[[applied]]
recipe = "interface"
name = "Clock"
package = ""
has_spec = false
files = [
  "src/main/java/com/example/demo/Clock.java",
]

[[applied]]
recipe = "test"
name = "Parser"
package = ""
has_spec = false
files = [
  "src/test/java/com/example/demo/ParserTest.java",
]
