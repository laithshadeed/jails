// Generated by jails from art_ent_payout_service. Clean hand edits survive regeneration.
package com.example.demo.service;

import com.example.demo.domain.Payout;
import com.example.demo.repository.PayoutRepository;
import java.util.List;
import java.util.Objects;
import java.util.Optional;
import java.util.UUID;
import org.springframework.stereotype.Component;

/**
 * What the application can do with {@link Payout}.
 *
 * <p>Depends on the port, not on an adapter, so a test can hand it an
 * in-memory implementation and never start a database.
 */
@Component
public class PayoutService {

    private final PayoutRepository repository;

    public PayoutService(PayoutRepository repository) {
        this.repository = Objects.requireNonNull(repository, "repository is required");
    }

    public Optional<Payout> byId(UUID id) {
        return repository.findById(id);
    }

    public List<Payout> all() {
        return repository.findAll();
    }

    public Payout save(Payout payout) {
        return repository.save(payout);
    }

    public boolean delete(UUID id) {
        return repository.deleteById(id);
    }

    // Reader-owned application methods belong below this stable boundary.
}
