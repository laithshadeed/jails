// Generated by jails from art_ent_payout_http_controller_test. Clean hand edits survive regeneration.
package com.example.demo.web;

import com.example.demo.domain.Payout;
import com.example.demo.domain.PayoutStatus;
import com.example.demo.repository.PayoutRepository;
import com.example.demo.service.PayoutService;
import java.time.Instant;
import java.util.List;
import java.util.Optional;
import java.util.UUID;
import org.junit.jupiter.api.Test;
import org.springframework.http.MediaType;
import org.springframework.test.web.servlet.assertj.MockMvcTester;
import static org.assertj.core.api.Assertions.assertThat;

class PayoutControllerTest {

    private static final Payout ROW = new Payout(UUID.fromString("00000000-0000-0000-0000-000000000001"), 1L, PayoutStatus.PENDING, 1L, Instant.parse("2026-01-01T00:00:00Z"));

    private static final PayoutRepository REPOSITORY = new PayoutRepository() {
        @Override
        public Optional<Payout> findById(UUID id) {
            return Optional.of(ROW);
        }

        @Override
        public List<Payout> findAll() {
            return List.of(ROW);
        }

        @Override
        public Payout save(Payout value) {
            return value;
        }

        @Override
        public boolean deleteById(UUID id) {
            return true;
        }
    };

    private static final String CREATE_REQUEST =
            """
            {
              "amount": 1,
              "status": "PENDING",
              "version": 1
            }""";

    private final MockMvcTester mvc = MockMvcTester.of(new PayoutController(new PayoutService(REPOSITORY)));

    @Test
    void theDocumentedCreateRequestIsAccepted() {
        assertThat(mvc.post().uri("/payouts")
                .contentType(MediaType.APPLICATION_JSON)
                .content(CREATE_REQUEST)).hasStatus(201);
        assertThat(mvc.get().uri("/payouts")).hasStatusOk();
    }

    // Reader-owned tests belong below this stable boundary.
}
