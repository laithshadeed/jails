// Generated by jails from art_ent_payout_repository_memory_test. Clean hand edits survive regeneration.
package com.example.demo.adapters.memory;

import com.example.demo.domain.Payout;
import com.example.demo.domain.PayoutStatus;
import com.example.demo.repository.PayoutRepositoryContract;
import java.time.Instant;
import java.util.UUID;
import org.junit.jupiter.api.Test;

/**
 * The fake is held to the same contract as the real adapter.
 *
 * <p>No database and no application context: the in-memory adapter is an
 * ordinary object with a constructor, which is the whole reason it exists.
 * The assertions live in {@code PayoutRepositoryContract} so this and the
 * JDBC adapter's integration test cannot come to disagree about them.
 */
class InMemoryPayoutRepositoryTest {

    @Test
    void satisfiesThePayoutRepositoryContract() {
        PayoutRepositoryContract.savesReadsAndDeletes(
                new InMemoryPayoutRepository(), new Payout(UUID.fromString("00000000-0000-0000-0000-000000000001"), 1L, PayoutStatus.PENDING, 1L, Instant.parse("2026-01-01T00:00:00Z")));
    }

    // Reader-owned cases belong below this stable boundary.
}
