// Generated by jails from art_ent_ticket_http_controller_test. Clean hand edits survive regeneration.
package com.example.demo.web;

import com.example.demo.domain.Ticket;
import com.example.demo.repository.TicketRepository;
import com.example.demo.service.TicketService;
import java.util.List;
import java.util.Optional;
import org.junit.jupiter.api.Test;
import org.springframework.test.web.servlet.assertj.MockMvcTester;
import static org.assertj.core.api.Assertions.assertThat;

class TicketControllerTest {

    private static final Ticket ROW = new Ticket(1L, "sample");

    private static final TicketRepository REPOSITORY = new TicketRepository() {
        @Override
        public Optional<Ticket> findById(long id) {
            return Optional.of(ROW);
        }

        @Override
        public List<Ticket> findAll() {
            return List.of(ROW);
        }

        @Override
        public Ticket save(Ticket value) {
            return value;
        }

        @Override
        public boolean deleteById(long id) {
            return true;
        }
    };

    private final MockMvcTester mvc = MockMvcTester.of(new TicketController(new TicketService(REPOSITORY)));

    @Test
    void theCollectionAnswers() {
        assertThat(mvc.get().uri("/tickets")).hasStatusOk();
    }

    // Reader-owned tests belong below this stable boundary.
}
