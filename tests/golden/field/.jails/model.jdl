jdl 1
app Demo @id(project_demo) {
  pkg com.example.demo
  java 26
  platform plain
  build maven
  storage none
}

entity Note @id(ent_note) {
  id: uuid @id(fld_note_id)
  title: string @id(fld_note_title) @notBlank
  createdAt: instant @id(fld_note_created_at)
}
