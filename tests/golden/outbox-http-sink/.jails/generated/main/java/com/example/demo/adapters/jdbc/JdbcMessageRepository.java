// Generated by jails from art_cap_db_ent_message_repository. Clean hand edits survive regeneration.
package com.example.demo.adapters.jdbc;

import com.example.demo.domain.Message;
import com.example.demo.repository.MessageRepository;
import java.sql.Timestamp;
import java.util.List;
import java.util.Optional;
import java.util.UUID;
import org.springframework.jdbc.core.simple.JdbcClient;
import org.springframework.stereotype.Repository;

@Repository
public final class JdbcMessageRepository implements MessageRepository {

    private final JdbcClient jdbc;

    public JdbcMessageRepository(JdbcClient jdbc) {
        this.jdbc = jdbc;
    }

    @Override
    public Optional<Message> findById(UUID id) {
        return jdbc.sql("select id, body, created_at from messages where id = :id")
                .param("id", id)
                .query(Message.class)
                .optional();
    }

    @Override
    public List<Message> findAll() {
        return jdbc.sql("select id, body, created_at from messages order by id")
                .query(Message.class)
                .list();
    }

    @Override
    public Message save(Message value) {
        return jdbc.sql("insert into messages (id, body, created_at) values (:id, :body, :created_at) on conflict (id) do update set body = excluded.body, created_at = excluded.created_at returning id, body, created_at")
                .param("id", value.id())
                .param("body", value.body())
                .param("created_at", Timestamp.from(value.createdAt()))
                .query(Message.class)
                .single();
    }

    @Override
    public boolean deleteById(UUID id) {
        return jdbc.sql("delete from messages where id = :id")
                .param("id", id)
                .update() > 0;
    }
}
