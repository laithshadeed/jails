// Generated by jails from art_ent_person_http_controller_test. Clean hand edits survive regeneration.
package com.example.demo.web;

import com.example.demo.domain.Person;
import com.example.demo.repository.PersonRepository;
import com.example.demo.service.PersonService;
import java.time.Instant;
import java.util.List;
import java.util.Optional;
import java.util.UUID;
import org.junit.jupiter.api.Test;
import org.springframework.http.MediaType;
import org.springframework.test.web.servlet.assertj.MockMvcTester;
import static org.assertj.core.api.Assertions.assertThat;

class PersonControllerTest {

    private static final Person ROW = new Person(UUID.fromString("00000000-0000-0000-0000-000000000001"), "sample", Instant.parse("2026-01-01T00:00:00Z"));

    private static final PersonRepository REPOSITORY = new PersonRepository() {
        @Override
        public Optional<Person> findById(UUID id) {
            return Optional.of(ROW);
        }

        @Override
        public List<Person> findAll() {
            return List.of(ROW);
        }

        @Override
        public Person save(Person value) {
            return value;
        }

        @Override
        public boolean deleteById(UUID id) {
            return true;
        }
    };

    private static final String CREATE_REQUEST =
            """
            {
              "email": "sample-email"
            }""";

    private final MockMvcTester mvc = MockMvcTester.of(new PersonController(new PersonService(REPOSITORY)));

    @Test
    void theDocumentedCreateRequestIsAccepted() {
        assertThat(mvc.post().uri("/people")
                .contentType(MediaType.APPLICATION_JSON)
                .content(CREATE_REQUEST)).hasStatus(201);
        assertThat(mvc.get().uri("/people")).hasStatusOk();
    }

    // Reader-owned tests belong below this stable boundary.
}
