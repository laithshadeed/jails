// Generated by jails from art_op_open_ticket_write_it. Clean hand edits survive regeneration.
package com.example.demo.adapters.jdbc;

import static org.assertj.core.api.Assertions.assertThat;

import com.example.demo.TestcontainersConfig;
import com.example.demo.application.commands.OpenTicketCommand;
import com.example.demo.domain.Ticket;
import org.junit.jupiter.api.Test;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.boot.test.context.SpringBootTest;
import org.springframework.context.annotation.Import;
import org.springframework.transaction.annotation.Transactional;

@Import(TestcontainersConfig.class)
@SpringBootTest
@Transactional
class JdbcOpenTicketCommandIT {

    @Autowired
    private OpenTicketCommand operation;

    @Test
    void writesThroughTheRealDatabase() {
        Ticket answered = operation.execute(new OpenTicketCommand.Input("sample"));

        // `returning` answers with the row the statement wrote, so an empty
        // answer here means it matched none -- which is the failure worth
        // catching.
        assertThat(answered).isNotNull();
    }

    // Reader-owned cases belong below this stable boundary.
}
