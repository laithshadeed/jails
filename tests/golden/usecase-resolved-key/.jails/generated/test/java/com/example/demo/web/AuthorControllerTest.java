// Generated by jails from art_ent_author_http_controller_test. Clean hand edits survive regeneration.
package com.example.demo.web;

import com.example.demo.domain.Author;
import com.example.demo.repository.AuthorRepository;
import com.example.demo.service.AuthorService;
import java.util.List;
import java.util.Optional;
import org.junit.jupiter.api.Test;
import org.springframework.http.MediaType;
import org.springframework.test.web.servlet.assertj.MockMvcTester;
import static org.assertj.core.api.Assertions.assertThat;

class AuthorControllerTest {

    private static final Author ROW = new Author(1L, "sample");

    private static final AuthorRepository REPOSITORY = new AuthorRepository() {
        @Override
        public Optional<Author> findById(long id) {
            return Optional.of(ROW);
        }

        @Override
        public List<Author> findAll() {
            return List.of(ROW);
        }

        @Override
        public Author save(Author value) {
            return value;
        }

        @Override
        public boolean deleteById(long id) {
            return true;
        }
    };

    private static final String CREATE_REQUEST =
            """
            {
              "email": "sample-email"
            }""";

    private final MockMvcTester mvc = MockMvcTester.of(new AuthorController(new AuthorService(REPOSITORY)));

    @Test
    void theDocumentedCreateRequestIsAccepted() {
        assertThat(mvc.post().uri("/authors")
                .contentType(MediaType.APPLICATION_JSON)
                .content(CREATE_REQUEST)).hasStatus(201);
        assertThat(mvc.get().uri("/authors")).hasStatusOk();
    }

    // Reader-owned tests belong below this stable boundary.
}
