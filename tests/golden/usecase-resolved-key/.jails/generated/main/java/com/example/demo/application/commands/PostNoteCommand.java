// Generated by jails from art_op_post_note_command. Clean hand edits survive regeneration.
package com.example.demo.application.commands;

import com.example.demo.domain.Note;
import java.util.Objects;

public interface PostNoteCommand {
    String ROUTE = "POST /customer_api/notes";

    Note execute(Input input);

    public record Input(
        String email,
        String body
    ) {

        public Input {
            Objects.requireNonNull(email, "email");
            email = email.trim();
            if (email.isEmpty()) {
                throw new IllegalArgumentException("email must not be blank");
            }
            Objects.requireNonNull(body, "body");
            body = body.trim();
            if (body.isEmpty()) {
                throw new IllegalArgumentException("body must not be blank");
            }
        }
    }
}
