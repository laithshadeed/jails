[tools]
# Jails generates production projects for the Java 25 LTS (`TARGET_RELEASE`).
# A pinned JDK 26 is used to test both that release target and explicit newer
# project choices; `javac --release 25` keeps generated bytecode on the LTS.
java = "openjdk-26.0.2"
maven = "3.9.16"
mvnd = "1.0.6"
